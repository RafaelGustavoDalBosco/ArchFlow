unit App.Common.Consts;

interface

const

{$REGION '| NOT FIND MESSAGES |'}
   SNotFind_PathOn_JSON = 'Não foi encontrado o campo %s no JSON!';
{$ENDREGION}

{$REGION '| CONFIRM MESSAGES |'}
   SConfirm_LeaveScreen = 'Deseja abandonar os dados digitados e sair da tela?';
{$ENDREGION}

{$REGION '| EMPTY MESSAGES |'}
   SWarn_Empty_FieldValue = 'O valor do campo %s está vazio!';
{$ENDREGION}

implementation

end.
